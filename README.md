Repo for practising C++ 

Flags for compilation: 
    g++ -Wall(all warnings) -Wextra(some extra warnings) -Wpedantic(follow standart) -Werror(turn all warnings as compilation error) -Wno-error=unused_value(remove warning from errors) file.cpp

